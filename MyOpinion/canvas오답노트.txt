함수를 겹쳐서 쓸 생각을 하지말고(기존에 했던 생각)
위에 var 객체를 만들어서 쓰면 될것 함수들안에서 다 계산한것을 가져다 쓰면될것
그리고 마우스 down이벤트가 작동하면 true false값을 전해주는 함수를만들고
다운일때 true 값,그리고 스타트 위치 지점값
그리고 mousemove쪽에서 끝나는 지점값을 설정하고
선을 다 그리고 난후에 스타트 지점값을 엔드 좌표값으로 설정을 바꿔주면될것
